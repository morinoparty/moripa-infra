[defaults]
inventory = inventory/hosts.yml
roles_path = roles
# community.sops.sops を有効化: *.sops.yml の変数を実行時に自動復号する
vars_plugins_enabled = host_group_vars,community.sops.sops
interpreter_python = auto_silent
host_key_checking = True
forks = 10

[ssh_connection]
pipelining = True
