- name: Linode ゲートウェイ(WireGuard ハブ + NAT + DNS + oauth2-proxy + Caddy + HAProxy)を構成
  hosts: gateway
  # 途中で失敗しても notify 済みの handler(nftables reload 等)を必ず実行する
  force_handlers: true
  roles:
    - base
    - wireguard
    - wg_dns
    - auth_proxy
    - reverse_proxy
    - tcp_proxy
