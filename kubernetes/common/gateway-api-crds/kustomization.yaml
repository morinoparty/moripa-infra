apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Cilium 1.19 がサポートするバージョン(versions.yml の gateway_api_version と一致させる)
  - https://github.com/kubernetes-sigs/gateway-api/releases/download/v1.4.1/standard-install.yaml
