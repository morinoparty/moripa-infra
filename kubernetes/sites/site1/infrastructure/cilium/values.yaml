# site1 固有の Cilium 設定(common/cilium/values.yaml の後に読み込まれ上書きする)

# この拠点の kube-vip VIP(cluster_lan_cidr 内。kube-proxy 不在でも API へ到達する
# ための直指定。group_vars/site1.yml の kube_vip_address と一致させる)
k8sServiceHost: 10.200.1.10
